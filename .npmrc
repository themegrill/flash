# stylelint-config-wordpress@12 declares a peer of stylelint ^7 while the build
# pins stylelint ^9; without this `npm install` (used by the QA suite in CI)
# refuses to resolve the tree.
legacy-peer-deps=true
